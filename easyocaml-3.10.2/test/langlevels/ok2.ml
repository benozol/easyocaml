let i = (M1.m1_val, M2.m2_val, M3.m3_val, M4.m4_val, M5.m5_val)

let (f,g) = (List.map, List.map)

let j = String.length "abc"

let p = (Stack.pop, Stack.pop)

let _ = Sys.command "echo \"everything allright!\""

let z = Int32.zero
